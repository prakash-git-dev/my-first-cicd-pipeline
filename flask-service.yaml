apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  labels:
    app: flask-app
spec:
  type: ClusterIP
  selector:
    app: flask-app
  ports:
  - name: http-flask
    port: 5000
    targetPort: 5000
  - name: http-nginx
    port: 80
    targetPort: 80
